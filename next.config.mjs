/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.licdn.com",
      "encrypted-tbn0.gstatic.com",
      "www.underfire.com",
    ],
  },
};

export default nextConfig;
